{% extends 'base.html.twig' %}

{% block title %}Hello WM!{% endblock %}

{% block body %}
    <script src="js/change_progress_bar_class.js"></script>
    {% include('inc/navbarWM.html.twig') %}


    <div class="column left">
        {% if categories %}
        <p class="h3 category-main">Categories:</p>
        {% for category in categories %}
            <p class="h5 category">{{ category }}</p>
        {% endfor %}
    </div>
    {% endif %}

    {% if wishmaps %}
        <div class="row column right" id="card">
            {% for wishmap in wishmaps %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <img class="card-img-top" style="max-width: 100%; height: auto"
                                 src="{{ asset('uploads/wish_map_images/' ~ wm.image) }}"
                                 alt="Card image cap">
                            <div class="progress">
                                <div id="progress-bar" role="progressbar"
                                     style="width: {{ wishmap.process }}%;" aria-valuenow="{{ wm.process }}"
                                     aria-valuemin="0" aria-valuemax="100">{{ wm.process }}%
                                </div>
                            </div>
                            <p class="card-text description">{{ wm.description }}</p>
                            <div class="row">
                                <label>Start date:</label>
                                {% if (wm.startDate is null) %}
                                    <p class="card-text"
                                       style="margin-left:10px "> --- </p>
                                {% else %}
                                    <p class="card-text"
                                       style="margin-left:10px ">{{ wm.startDate|date('d-m-Y')|e }}</p>
                                {% endif %}
                                <label style="margin-left:10px ">Finish date:</label>
                                <p class="card-text"
                                   style="margin-left:10px ">{{ wm.finishDate|date('d-m-Y') }} </p>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

    {% else %}
        <p>You have no WISHES!</p>
    {% endif %}



{#
    <div class="someclass">{{ knp_pagination_render(wishmaps) }}</div>
#}


    <script src="js/person_search.js"></script>
{% endblock %}
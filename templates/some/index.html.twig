{% extends 'base.html.twig' %}

{% block title %}Hello SomeController!{% endblock %}

{% block body %}
    {% if app.user !=null %}
        <style>
            .example-wrapper {
                margin: 1em auto;
                max-width: 800px;
                width: 95%;
                font: 18px/1.5 sans-serif;
            }

            .example-wrapper code {
                background: #F5F5F5;
                padding: 2px 6px;
            }
        </style>

        <div class="example-wrapper">
            <h1>Hello {{ controller_name }}! ✅</h1>

            This friendly message is coming from:
            <ul>
                <li>Your controller at <code><a
                                href="{{ 'C:/OpenServer/domains/eshop/src/Controller/SomeController.php'|file_link(0) }}">src/Controller/SomeController.php</a></code>
                </li>
                <li>Your template at <code><a
                                href="{{ 'C:/OpenServer/domains/eshop/templates/some/index.html.twig'|file_link(0) }}">templates/some/index.html.twig</a></code>
                </li>
            </ul>
        </div>
    {% else %}
        <a href="{{ path('app_login') }}" class="btn btn-info">
            You need to login to see this page </a
        >
    {% endif %}
{% endblock %}
